function findNeedle(haystack){
let hay;
  for(let i = 0; i < haystack.length; i++){
    if(haystack[i] == 'needle'){
      hay = i
    }else if(haystack[i] == 'a dog'){
      hay = i
  }
}
  return `found the needle at position ${hay}`
}
